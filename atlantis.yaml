version: 3
automerge: false
parallel_plan: true
parallel_apply: false

projects:
  - name: lineten-dev
    dir: projects/lineten/dev
    workspace: default
    autoplan:
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - "../../../modules/**/*.tf"
      enabled: true
    apply_requirements:
      - mergeable
